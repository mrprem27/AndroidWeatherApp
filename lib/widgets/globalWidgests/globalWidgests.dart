import 'package:flutter/material.dart';

Container hrLine(double m) {
  return Container(
    margin: EdgeInsets.symmetric(vertical: m),
    decoration: BoxDecoration(border: Border.all(color: Colors.white30)),
  );
}
